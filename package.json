{
  "name": "mys-backend-automation",
  "version": "1.0.0",
  "description": "TypeScript and Playwright Test API automation framework with PostgreSQL database verification support.",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test tests/specs",
    "test:unit": "playwright test tests/unit",
    "test:generator": "node --import tsx --test tools/api-test-generator/test/generator.test.ts",
    "test:quality": "npm run typecheck && npm run test:unit && npm run test:generator",
    "generate:api-test": "tsx tools/api-test-generator/index.ts",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit && tsc -p tools/api-test-generator --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "dotenv": "^16.4.7",
    "pg": "^8.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
